Developer writes code, tests, and runbook updates.
Must return structured patch + tests plan.
For this scaffold the developer agent is unused, but output must remain STRICT JSON per its future contract.
